Github 협업 방법

기본적인 명령어

1.init
명령어 실행 시, .git 디렉토리가 생성(숨김파일)되며, git에 의해 관리된다. 숨김파일이기 때문에 "git init"으로 접근해야한다.
2.add
명령어를 실행하는 방법은 "git add (파일명)" 파일의 변경사항 추가 시 사용한다. 수정 작업을 한 후 commit 메세지를 통해 변경사항을 기록할 수 있다.
3.git commit -m "(변경사항을 입력한다)"
4.remote
사용되는 명령어는 "remote add". git remote add origin (저장소의 url주소) commit 후 내용을 원격 저장소에 올릴 수 있다.
이 때, 저장소 url을 지정해야한다. 지정하는 방식은 예를 들어, github에서 무료로 공개된 저장소에 저장하게되면 레포지토리를 생성, url확인 후 git과 
연결한다. https://github.com/(유저ID)/(git이름).git~
5.push, pull
push는 PC의 소스코드를 remote로 연결된 url로 업로드한다. pull은 반대로 url에 있는 파일을 내 pc로 가져온다.
"git pull" / "git push origin master"

